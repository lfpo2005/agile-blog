server:
  port: 8087
  forward-headers-strategy: native
  servlet:
    context-path: '/blog'

spring:
  datasource:
    url:  jdbc:postgresql://localhost:5432/agileblog
    username: postgres
    password: postgres
  type: com.zaxxer.hikari.HikariDataSource
  hikari:
    connection-timeout: 20000
    minimum-idle: 200
    maximum-pool-size: 550
    idle-timeout: 10000
    pool-name: PoolHikari
    auto-commit: false
  dbcp2:
    pool-prepared-statements: true
    max-open-prepared-statements: 550
  transaction:
    rollback-on-commit-failure: true
  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.PostgreSQLDialect
      jdbc:
        lob.non-contextual-creation: true
    properties:
      hibernate:
        show_sql: true
  output:
    ansi:
      enabled: Always
  mail:
    host: smtplw.com.br
    port: 587
    username: contato@metodologia-agil.com.br
    password: 'paRGQbTw6874'
    protocol: smtp
    sender:
      name: Blog agil
      email: contato@metodologia-agil.com.br
    properties:
      smtp:
        auth: true
        ssl:
          enable: true
  cache:
    type: caffeine
    caffeine:
      specs:
        postAllCache: maximumSize=500,expireAfterWrite=2m
  mvc:
    throw-exception-if-no-handler-found: true
    resources:
      add-mappings: false


logging:
  level:
    com.blog: TRACE
    root: INFO
    org.springframework.cache: DEBUG
    org.springframework.web: DEBUG
    org.hibernate: INFO

blog:
  auth:
    jwtSecret: t7w!z%C*F-JaNdRgUkXp2s5v8x/A?D(G+KbPeShVmYq3t6w9z$B&E)H@McQfTjWn
    jwtExpirationMs: 14400000